@AGENTS.md

<!-- Claude-only notes below this line. AGENTS.md is the source of truth for
     agent instructions; VS Code/Copilot and other harnesses read it directly. -->

- The gummy-berry-juce plugin provides the skills (`rt-safety`, `dsp-design`, `juce-ui`, `plugin-planning`, `build-and-validate`, `consult`), agents (`researcher`, `code-reviewer`, `bug-investigator`, `mechanic`), and commands (`/new-plugin`, `/feature`, `/fix`, `/gate`) referenced in AGENTS.md.
